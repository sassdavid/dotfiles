theme = Gruvbox Dark

font-family = JetBrainsMono Nerd Font
font-size = 11
font-feature = -calt, -liga, -dlig

cursor-style-blink = false
cursor-text = #000000

# Load a custom config for overrides, ? silently fails if the file is missing.
config-file = ?config.local
